.tabs_title {
  display: flex;
  justify-content: center;
  align-items: center;
}
.title_item{
  padding: 15rpx 0;
  flex: 1;
  text-align: center;
}
.active{
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}